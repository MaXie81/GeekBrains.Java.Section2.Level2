С момента последнего состоявшегося занятия(11.06), сделал:
1. Разобрался с FlyWay. 
ИМХО, очень "сырая" утилита. Мне удалось только с третьей версии ее "прикрутить". 
7-ая(последняя версия) не имеет конструктора, который создает объект Flyway класса через 3 основных параметра: URL, логин, пароль - требует создания отдельного объекта класса Configuration(если не ошибаюсь). 
5-ая версия, удивительно, но не работает с 11-ой версией Oracle XE - только платная версия FlyWay сохранила такую возможность. 
И только установив 4-ую версию, удалось добиться установления связи FlyWay с СУБД. 
По своему опыту, создание в Oracle таблиц в нижнем регистре - не "комильфо"(обращение к таким таблицам только через заключение их имен в двойные ковычки). Строгая организация вложенных папок(не через параметры), наименования файлов-миграций(считать кол-во пробелов в имени файла) - все это не производит впечатления какого то "промышленного стандарта". Но разобрался, буду использовать.

2. Написал несколько проектов, в которых реализован код модулей-прототипов(заготовки), который в дальнейшем получит "кастомизированное" развитие.
2.1 Навигация по Файловой системе, с передачей данных о файлах/каталогах текущей папки и выбором файла или переходом в другой каталог;
2.2 Организация сетевого взаимодействия между Клиентом и Сервером, только подключение(пока без авторизации);
2.3 Чтение/запись содержимого файла, отработка компонента передачи данных файла по сети;
2.4 Блок взаимодействия с СУБД для хранения и передачи данных(пока использовать в проекте не планирую, но если позволит время, то реализую и этот компонент приложения).